import 'package:flutter/material.dart';

class PageTwo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        color: Colors.teal,
        child: Text('Page Two', style: TextStyle(fontSize: 28.0),),
      ),
    );
  }
}
