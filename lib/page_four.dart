import 'package:flutter/material.dart';

class PageFour extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        color: Colors.indigoAccent,
        child: Text('Page Four', style: TextStyle(fontSize: 28.0),),
      ),
    );
  }
}
